# Vercel Deployment Requirements
# Optimized for serverless deployment

# Core API
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# ML Core (smaller versions for Lambda size limits)
numpy==1.24.3
scikit-learn==1.3.2
xgboost==2.0.3
joblib==1.3.2

# Data processing
pandas==2.0.3

# Interpretability (optional - may exceed size limits)
# shap==0.43.0  # Comment out if Lambda size exceeded
# lime==0.2.0.1

# If you need SHAP/LIME, consider using Vercel Pro or splitting into microservices

